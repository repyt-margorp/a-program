# Archived Artifact Schemas

The schemas in this directory document historical artifact formats only.

They are not accepted by the active reader and must not be used as parser
fallbacks. The active accepted proof artifact schema is
`../artifact_v86.schema`; the distinct checked semantic container is specified
by `../checked_artifact_v87.schema`.
