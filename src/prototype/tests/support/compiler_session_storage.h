#ifndef A_PROGRAM_PROTOTYPE_TEST_COMPILER_SESSION_STORAGE_H
#define A_PROGRAM_PROTOTYPE_TEST_COMPILER_SESSION_STORAGE_H

/* Test-only structural inspection boundary. Production code must use the
 * opaque session API and narrow Layer C/Layer T capabilities instead. */
#include "../../src/driver/compiler_session_internal.h"

#endif
