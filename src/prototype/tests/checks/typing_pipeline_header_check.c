#include "a_program/frontend/typing_pipeline.h"

/* The persistent Layer T owner must be usable without any Core storage,
 * provider, request, or snapshot header. */
int main(void) {
	struct prototype_typing_pipeline* pipeline = 0;
	return pipeline != 0;
}
